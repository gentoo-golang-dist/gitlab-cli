---
title: '`glab runner create`'
stage: Create
group: Code Review
info: To determine the technical writer assigned to the Stage/Group associated with this page, see <https://handbook.gitlab.com/handbook/product/ux/technical-writing/#assignments>
---

<!--
This documentation is auto generated by a script.
Please do not edit this file directly. Run `make gen-docs` instead.
-->

Create a CI/CD runner linked to your user.

## Synopsis

Creates a new runner linked to your user. The new runner is
tied to your user for ownership and audit.

```plaintext
glab runner create [flags]
```

## Examples

```console
# Instance runner (admin)
glab runner create --runner-type instance_type --description "shared runner"

# Group runner
glab runner create --runner-type group_type --group-id 123 --description "group ci"

# Project runner with tags and JSON output
glab runner create --runner-type project_type --project-id 456 --tag docker --tag linux --output json
```

## Options

```plaintext
      --access-level string       Access level: "not_protected" or "ref_protected".
  -d, --description string        Description of the runner.
      --group-id int              Group ID (required when runner-type is group_type).
  -l, --locked                    When set, the runner is locked to the current project.
      --maintenance-note string   Maintenance note (up to 1024 characters).
      --maximum-timeout int       Maximum job duration in seconds for this runner.
  -F, --output string             Format output as: text, json. (default "text")
  -p, --paused                    When set, the runner ignores new jobs.
      --project-id int            Project ID (required when runner-type is project_type).
  -R, --repo OWNER/REPO           Select another repository. Can use either OWNER/REPO or `GROUP/NAMESPACE/REPO` format. Also accepts full URL or Git URL.
  -u, --run-untagged              When set, the runner processes jobs without tags.
      --runner-type string        Runner scope: "instance_type", "group_type", or "project_type".
      --tag strings               Runner tags. Repeat the flag or use a comma-separated list.
```

## Options inherited from parent commands

```plaintext
  -h, --help   Show help for this command.
```
