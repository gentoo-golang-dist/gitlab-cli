---
title: '`glab service-account token list`'
stage: Create
group: Code Review
info: To determine the technical writer assigned to the Stage/Group associated with this page, see <https://handbook.gitlab.com/handbook/product/ux/technical-writing/#assignments>
---

<!--
This documentation is auto generated by a script.
Please do not edit this file directly. Run `make gen-docs` instead.
-->

List personal access tokens for a service account.

## Synopsis

List all personal access tokens for a service account in a group.

The output contains the token's metadata, not the actual token value.

```plaintext
glab service-account token list [flags]
```

## Aliases

```plaintext
ls
```

## Examples

```console
# List tokens for a service account
glab service-account token list --service-account my-bot --group my-group

# List only active tokens as JSON
glab service-account token list --service-account my-bot --group my-group --active --output json
```

## Options

```plaintext
  -a, --active                   List only active tokens.
  -g, --group string             The group the service account belongs to (required).
  -F, --output string            Format output as: text, json. (default "text")
  -R, --repo string              Select another repository. You can use either OWNER/REPO or GROUP/NAMESPACE/REPO. The full URL or Git URL is also accepted.
      --service-account string   The service account name or numeric ID (required).
```

## Options inherited from parent commands

```plaintext
  -h, --help   Show help for this command.
```
