---
title: '`glab service-account token rotate`'
stage: Create
group: Code Review
info: To determine the technical writer assigned to the Stage/Group associated with this page, see <https://handbook.gitlab.com/handbook/product/ux/technical-writing/#assignments>
---

<!--
This documentation is auto generated by a script.
Please do not edit this file directly. Run `make gen-docs` instead.
-->

Rotate a personal access token for a service account.

## Synopsis

Rotate a personal access token for a service account in a group.
This revokes the existing token and creates a new one.

If multiple tokens share the same name, specify the token ID
to select the correct one.

The new token expires at 00:00 UTC on a date calculated by adding
the duration to today's date. The default duration is 30 days.

```plaintext
glab service-account token rotate <token-name|token-id> [flags]
```

## Aliases

```plaintext
rot
```

## Examples

```console
# Rotate a token (default 30 day expiry)
glab service-account token rotate my-token --service-account my-bot --group my-group

# Rotate with explicit expiration date
glab service-account token rotate my-token --service-account my-bot --group my-group --expires-at 2025-12-31

# Rotate with 90 day lifetime
glab service-account token rotate my-token --service-account my-bot --group my-group --duration 90d
```

## Options

```plaintext
  -D, --duration duration        Sets the token lifetime. Accepts: days (30d), weeks (4w), or hours in multiples of 24 (24h). Maximum: 365d. (default 30d)
  -E, --expires-at DATE          Sets the token's expiration date, in YYYY-MM-DD format. If not specified, --duration is used. (default 0001-01-01)
  -g, --group string             The group the service account belongs to (required).
  -F, --output string            Format output as 'text' for the new token value, 'json' for the full token object. (default "text")
  -R, --repo string              Select another repository. You can use either OWNER/REPO or GROUP/NAMESPACE/REPO. The full URL or Git URL is also accepted.
      --service-account string   The service account name or numeric ID (required).
```

## Options inherited from parent commands

```plaintext
  -h, --help   Show help for this command.
```
